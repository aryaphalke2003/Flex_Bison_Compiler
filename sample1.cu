int func(int x,int y);
int maximum(int x, int y) {
        // This will compare 2 numbers
        if (x > y)
            return x;
        else {
            return y;
        }
    }
int main() 
{
maximum(2,3);
}
